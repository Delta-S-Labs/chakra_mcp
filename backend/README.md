# backend (ChakraMCP relay network)

The Rust implementation of the ChakraMCP relay lives here. See
[`../docs/chakramcp-build-spec.md`](../docs/chakramcp-build-spec.md) for the full build
specification — tech stack, data model, API endpoints, phased implementation order, and AWS
deployment shape.

This directory is a placeholder until we start Phase 1 of the backend. The frontend ships first.
